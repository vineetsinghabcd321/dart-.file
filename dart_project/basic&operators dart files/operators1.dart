//Write a Dart program to calculate the area of a rectangle using variables for length and width. Print the result.

void main() {
  int length = 6, width = 9;
    int area = length * width;
     print(area );
}